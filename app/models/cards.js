function Card(options) {
  this.options = options;
  // let color = options.color;
  // let cost = options.cost;
  // let name = options.name;
  // let freeIf = options.freeIf;
  // let futureZeroCostModifierCard = null;
  // let victoryPoints = 0
  // let shieldValue = 0
  // let scientistsGuildValue
  // let coinsPerTurn = 0 // the card gives player n coins every turn
  // let immediateCoins = 0 // the card gives player n coins immediately

  let directions = [
    "left", "right", "self"
  ]

  // can be per resource RAW GOODs or MANUFACTURED GOODS
  let tradeModifiers = undefined

  // resource per turn
  let resourcesPerTurn = [
  ]

  // Player can produce 
  let nonPurchasableResourcesPerTurn = [
  ]

}